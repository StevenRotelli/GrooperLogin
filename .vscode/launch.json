{

    "version": "0.2.0",
    "configurations": [
        {
            "name": "PowerShell: Launch Installer",
            "type": "PowerShell",
            "request": "launch",
            "script": "Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass; ${cwd}/install.ps1",
            
            "createTemporaryIntegratedConsole": true,
        }
    ]
}
